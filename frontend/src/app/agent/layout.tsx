/**
 * Legacy /agent route — page redirects to /overview + Agent side panel.
 * No AgentLayoutShell here (full-page Agent dashboard is deprecated).
 */
export default function AgentLayout({ children }: { children: React.ReactNode }) {
  return children;
}
