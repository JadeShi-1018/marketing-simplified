'use client';

// /projects/<key>/tasks/<taskSlug> renders the same tasks list page, which reads
// the taskSlug path segment to open that task's drawer over the list.
export { default } from '../page';
