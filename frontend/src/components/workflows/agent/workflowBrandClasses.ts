/** Shared brand gradient styles for the workflows/agent UI. */

/** Compact variant for hover card footer Template button. */
export const brandBtnSm =
  "flex shrink-0 items-center gap-1 rounded-lg bg-brand-gradient px-2.5 py-1 text-xs font-medium text-white transition hover:opacity-95"

/** Toolbar Template button (slightly larger padding). */
export const brandBtnToolbar =
  "flex items-center gap-1.5 rounded-xl bg-brand-gradient px-3 py-1.5 text-sm font-medium text-white shadow-sm transition hover:opacity-95"

/** Page header Create workflow button. */
export const brandBtnHeader =
  "inline-flex items-center gap-2 rounded-xl bg-brand-gradient px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:opacity-95 focus:outline-none focus:ring-2 focus:ring-[#3CCED7]/40 focus:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-60"

/** Active tab: brand gradient pill, white text. */
export const brandTabActive =
  "rounded-lg bg-brand-gradient px-4 py-2 text-sm font-medium text-white shadow-sm transition hover:opacity-95"

/** Inactive tab: gray text only (no border). */
export const brandTabInactive =
  "rounded-lg px-4 py-2 text-sm font-medium text-gray-500 transition hover:text-gray-800"
